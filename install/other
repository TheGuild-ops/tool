sudo apt update && apt upgrade -y
sudo apt install make clang pkg-config libssl-dev build-essential git curl ntp jq llvm -y < "/dev/null"
sudo apt install glances -y
sudo apt-get install build-essential ocaml ocamlbuild automake autoconf libtool wget python libssl-dev git cmake perl tmux ufw gcc unzip zip jq make -y < "/dev/null"
sudo apt-get install golang-statik -y
sudo apt update && apt upgrade -y
echo "we open"
echo ${arr[*]}
echo "port"
